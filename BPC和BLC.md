1. BLC (Black Level Correction) - 黑电平矫正

作用：矫正传感器（Sensor）的暗电流。即使在完全无光的环境下，Sensor的像素点也会输出一个非零的电平值（即黑电平或光学黑区OB值）。BLC的作用就是减去这个固定的偏移量，使纯黑区域的像素值归零。
原理：

Sensor的模拟信号在转换为数字信号时，由于电路特性和精度限制，会存在一个基础电平。
这个电平值与Sensor的增益（Gain）、曝光时间、温度等因素相关。
BLC为R、Gr、Gb、B四个通道分别配置一个偏移量，从RAW数据中减去，确保图像从正确的基准开始处理。

调试影响：如果BLC配置错误，会导致图像整体偏色（如偏绿、偏紫） 或暗部细节异常。它是所有后续处理的基础。

2. BPC (Bad Pixel Correction) - 坏点矫正

作用：检测并修复图像传感器上的坏点（Dead/Hot Pixel）。
原理：

由于制造工艺缺陷或长期使用，Sensor上某些像素点可能永远输出高值（亮点）、低值（暗点）或随机值。
BPC算法通过比较当前像素与其周围同通道像素的数值。
如果当前像素值与邻域像素的统计值（如中值、均值）差异超过预设阈值，则判定为坏点。
使用邻域正常像素的值（如中值滤波）来替换坏点的值。

展锐使用的方法是中值滤波的方法

调试影响：有效去除图像中固定的白点、黑点或彩点，提升画面纯净度。过度矫正可能损失细节。