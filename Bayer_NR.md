# bayer_nr
```
Bayer NR 模块通过 3 个亮度区域和 4 个频率区域划分，将图像的每个像素点分类到 12 个区域里面。待调
优图像像素点划分到合理的区域后，再使用合理的降噪强度参数，以达到较好的降噪效果。在调试的过
程中，会借助查看 MAP 图，获取 MAP 图信息来合理地修改对应的参数
```
## 如何标定
```
1.在D65光源下，设置光源亮度为最大亮度
将24色卡放置灯箱中进行拍摄，调整灰卡距离，确保灰色部分位于下方，同时将色卡居中
```
<font color="red">VST作用：</font>得到VST和IVST两张表用做噪声变换
```
2.VST的sensor Raw图
使用手动曝光的方式设置曝光时间以及增益，增益设置为128/192/256/320
保持色卡19色块bv处于190-220之间，拍摄色卡保存senser Raw图
将图片放到对应的地方工具便可以自动计算了
```
<font color="red">SAD作用：</font>计算不同亮度段下的频率阈值

```
3.SAD的Dcam RAW图
使用手动曝光的方式设置曝光时间以及增益，增益设置为128/256/512/1024/2048/4096/8192
保持色卡19色块bv处于190-220之间，拍摄色卡保存Dcam Raw图
将图片放到对应的地方，框选24色卡工具便可以自动计算了
```

<font color="red">标定RNR作用：</font>更具LSC table表得到沿径向去噪的保护半径，速率，截止值以及回加噪声的保护半径

```
4.RNR标定
RNR的标定需要调用Alsc标定时DNP灯光下的ALSC table表，在此之前请先完成ALSC标定。
选择DNP光源对应的文件[00]。
此时Gain档位1x,2x,4x,8x,16x,32x,64x下的部分数据会更新其中radius和radius_ld和标定得出radius和radius_ld保持一致

```



## 如何调试
```
1.根据图像的gain值选着对应的region的参数列表，后开启bayer_nr模块
```
![](./phone/bayer_nr.png)
```
2.调节亮度区域划分参数，确保合理的分配三个亮度区。具体亮度情况可以通过lum_flag_maop图查看划分是否合理
```
```
3.调试频率划分参数，将所有的像素划分到不同的频率区域，以适配不同的降噪强度。具体频率划分可以通过flag_maop图查看划分是否合理
```

```
4.调节降噪强度
5.开启沿径向降噪模块，首先确定保护半径是否合理，再更具四角噪声的情况调节降噪递增变化的强度
```
```
6.最后根据噪声情况，确定图像纹理是否有被抹去态度，可以通过调节回加噪声，来增加细节。但需要注意的是噪声同时也会增加
```
```
7.根据图像边沿噪声情况，调节边沿线的对比度
```

```
如何平衡噪声和清晰度?
首先保证在没有回加噪声的情况下保证画面干净,噪声少,之后利用回加噪声补回部分噪声,当回补的噪声没办法满足清晰度时再去减少一些去噪强度,适当放出噪声.核心在于避免假纹理,锯齿,以及噪点的产生
高亮处可以增加回加噪声,低亮处对于噪声的回加就要非常谨慎了.
当回加噪声太强的时候,需要先判断是回加的黑白边太宽还是还是黑白边亮度太高.又或者是整体细节增强的比较多
```
```
Bayer NR模块作用于RAW（Bayer）域，是ISP处理流程中首个降噪模块。
其核心原理是基于图像搜索窗口，通过比较中心块与相邻块的相似性来获取降噪权重，并进行加权平均。

相似性权重：搜索窗口内，与中心块越相似的相邻块，被赋予的降噪权重越大。
自适应降噪：模块会根据当前像素点所处的纹理程度，自适应地修改降噪权重的大小，从而在去噪的同时更好地保护图像细节。

2. 算法流程与分区策略
为了实现精细化的降噪控制，Bayer NR采用了分亮度、分频域、沿径向的多维度分区策略：

分亮度 (Luminance Partitioning)：

将图像像素根据亮度划分为3个区域：低亮区（lum0）、中亮区（lum1）、高亮区（lum2）。
通过参数 lum_T0 和 lum_T1 设定亮度阈值进行划分。

分频域 (Frequency Partitioning)：

在每个亮度区域内，再根据局部纹理复杂度（梯度），将像素划分为4个频率区域：平坦区域（Flat0）、次平坦区域（Flat1）、次纹理区域（Flat2）、纹理区域（Flat3）。
通过参数 flat0_thresh、flat1_thresh 等设定频率阈值进行划分。
降噪原则：对不同频率区域施加不同的降噪强度。通常，平坦区域（Flat0）施加最强的降噪，纹理区域（Flat3）施加最弱的降噪甚至进行细节回加，以保护边缘和纹理细节。

沿径向降噪 (Radial Noise Reduction)：

目的：由于镜头 shading 效应，图像四角通常比中心更暗，在提亮后噪声会更明显。此功能旨在对图像边缘和四角区域施加更强的降噪。
机制：设定一个保护半径（Radius）。在半径内的中心区域使用基础降噪强度以保护细节；在半径外，降噪强度随着像素点远离图像中心而逐渐增强（通过调整 flat[0,2]_Tcoef 等参数实现）。

MAP图辅助调试：

flat_map：显示每个像素被划分到了哪个频率区域（Flat0/1/2）。调试时可观察平坦区域和纹理区域的划分是否准确，例如纹理细节是否被误判为平坦区。
radius_map：显示沿径向降噪的保护半径区域。用于确认 Radius 参数设置是否合理，保护区域是否覆盖了图像中心主要区域。
lum_flag_map：结合了亮度和频率信息的MAP，用于综合判断分区效果。

```